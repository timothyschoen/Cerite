#include <string.h>
#include <unistd.h>
#include <tclap/CmdLine.h>




int main(int argc, char* argv[])
{
        /*

            TCLAP::CmdLine cmd("Circuit", ' ', "0.2");
            TCLAP::ValueArg<std::string> outputFile("o","output", "choose output file", false, "./output.wav", "path");
            TCLAP::SwitchArg rtswitch("r", "realtime", "enable realtime playback");
            cmd.add(outputFile);
            cmd.add(rtswitch);
            cmd.parse(argc, argv);
            std::string outputpath = outputFile.getValue();
            int realtime = rtswitch.getValue();

            audio_init();

            if(realtime == 0) exportFile(outputpath);
            else realtime_audio();

         */
        //



}
